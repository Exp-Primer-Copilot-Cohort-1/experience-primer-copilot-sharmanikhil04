function skillsMember() {
    // Get the element with id="skills"
    var element = document.getElementById("skills");
    // If the element with id="skills" exists, then add the class "member" to it
    if (element) {
        element.classList.add("member");
    }
    }   